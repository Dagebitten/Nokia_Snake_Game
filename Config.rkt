#lang racket/gui
;File for setting the GENERAL SETTINGS for MAIN CODE

(provide SCREEN_WIDTH SCREEN_HEIGHT SQUARE_SIZE DEFAULT_SCALE DEBUG? FPS)
(define SQUARE_SIZE 5)
(define DEBUG? #f)
(define DEFAULT_SCALE 2)
(define SCREEN_HEIGHT 300)
(define SCREEN_WIDTH 500)
(define FPS 15)
